# Final Transfer Practicum

这里不是 M14，也不引入新的课程知识。

M00–M13 一直在 TaskForge 中训练 system modeling、contract、evidence、safe change、review 与 Agent authority。Final Transfer Practicum 刻意换到一个**真实、陌生、非 TaskForge** 的开源代码库，检查这些能力是否真的能迁移出去。

当前 practicum：

- [`final-transfer-click/`](final-transfer-click/README.md) — 基于 Pallets Click 的 frozen historical revision；domain 是 Python CLI library / testing tooling，而不是后台 job system。

完成 M13 后再做。不要把这里当作新的教材章节顺读；它是一份低 scaffolding 的最终工程任务。
